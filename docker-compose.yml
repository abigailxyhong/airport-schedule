services:

  api:
    build: .
    command: uvicorn api:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"

  dashboard:
    build: .
    command: python app.py
    ports:
      - "8050:8050"
    depends_on:
      - api

    environment:
    -  API_URL=http://api:8000